[[ _________ .___ / _____/__.__. __| _/____ _____ ______________ \_____ < | |/ __ |/ __ \\__ \\_ __ \_ __ \ / \___ / /_/ \ ___/ / __ \| | \/| | \/ /_______ / ____\____ |\___ >____ /__| |__| \/\/ \/ \/ \/ ]] local IMAGE_ID = "rbxassetid://72980606617928" local SIZE = UDim2.new(0, 500, 0, 500) local FADE_TIME = 0.4 local SHOW_TIME = 1 local CoreGui = game:GetService("CoreGui") local TweenService = game:GetService("TweenService") local Players = game:GetService("Players") local HttpService = game:GetService("HttpService") local TextService = game:GetService("TextService") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local GuiService = game:GetService("GuiService") local player = Players.LocalPlayer local playerGui = player:WaitForChild("PlayerGui") local function isMobile() return UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled end local IS_MOBILE = false local gui = Instance.new("ScreenGui") gui.Name = "PopupImageGUI" gui.IgnoreGuiInset = true gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling gui.Parent = CoreGui local img = Instance.new("ImageLabel") img.AnchorPoint = Vector2.new(0.5, 0.5) img.Position = UDim2.new(0.5, 0, 0.5, 0) img.Size = SIZE img.BackgroundTransparency = 1 img.Image = IMAGE_ID img.ImageTransparency = 1 img.Parent = gui local fadeIn = TweenService:Create(img, TweenInfo.new( FADE_TIME, Enum.EasingStyle.Quad, Enum.EasingDirection.Out ), { ImageTransparency = 0 }) fadeIn:Play() fadeIn.Completed:Wait() task.wait(SHOW_TIME) local fadeOut = TweenService:Create(img, TweenInfo.new( FADE_TIME, Enum.EasingStyle.Quad, Enum.EasingDirection.In ), { ImageTransparency = 1 }) fadeOut:Play() fadeOut.Completed:Wait() gui:Destroy() wait() local DEEPSEEK_API_KEY = "sk-78fb2cccb9e446bdb582ff52cdfc74a8" local DEEPSEEK_API_URL = "https://api.deepseek.com/v1/chat/completions" local RATE_LIMIT_DELAY = 15 local MAX_MESSAGE_LENGTH = 300 local MAX_MESSAGES_PER_DAY = 100 local lastMessageTime = 0 local conversations = {} local activeConversationId = 1 local conversationCounter = 1 local messageCountToday = 0 local lastResetDate = os.date("*t").day local aiModels = { "deepseek-chat", "deepseek-reasoner", } local ANIMATION_SPEED = 0.3 local EASE_STYLE = Enum.EasingStyle.Quint local EASE_DIRECTION = Enum.EasingDirection.Out local COLOR_SCHEME = { background = Color3.fromRGB(52, 53, 65), sidebar = Color3.fromRGB(32, 33, 35), input = Color3.fromRGB(64, 65, 79), userMessage = Color3.fromRGB(147, 51, 234), aiMessage = Color3.fromRGB(68, 70, 84), accent = Color3.fromRGB(16, 163, 127), text = Color3.fromRGB(236, 236, 241), textSecondary = Color3.fromRGB(172, 172, 190), hover = Color3.fromRGB(42, 43, 50), error = Color3.fromRGB(239, 65, 70), purple = Color3.fromRGB(147, 51, 234) } local GUI_SIZE = IS_MOBILE and UDim2.new(0.95, 0, 0.85, 0) or UDim2.new(0, 400, 0, 300) local SIDEBAR_WIDTH = IS_MOBILE and 0 or 260 local logoGui = Instance.new("ScreenGui") logoGui.Name = "SydearrChatbotLogo" logoGui.ResetOnSpawn = false logoGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling logoGui.Parent = playerGui local logoFrame = Instance.new("Frame") logoFrame.Size = UDim2.new(1, 0, 1, 0) logoFrame.BackgroundColor3 = COLOR_SCHEME.background logoFrame.BorderSizePixel = 0 logoFrame.Parent = logoGui local logoImage = Instance.new("ImageLabel") logoImage.Size = UDim2.new(0, 200, 0, 200) logoImage.Position = UDim2.new(0.5, -100, 0.5, -100) logoImage.BackgroundTransparency = 1 logoImage.Image = "rbxassetid://80592873690386" logoImage.Parent = logoFrame logoFrame.BackgroundTransparency = 1 logoImage.ImageTransparency = 1 local function tweenProperty(instance, property, targetValue, duration, easingStyle, easingDirection) local tweenInfo = TweenInfo.new( duration or ANIMATION_SPEED, easingStyle or EASE_STYLE, easingDirection or EASE_DIRECTION ) local tween = TweenService:Create(instance, tweenInfo, {[property] = targetValue}) tween:Play() return tween end spawn(function() logoGui:Destroy() end) local function saveConversations() local data = { conversations = conversations, conversationCounter = conversationCounter, messageCountToday = messageCountToday, lastResetDate = lastResetDate } writefile("sydearr_chatbot_conversations.json", HttpService:JSONEncode(data)) end local function loadConversations() if isfile("sydearr_chatbot_conversations.json") then local success, data = pcall(function() return HttpService:JSONDecode(readfile("sydearr_chatbot_conversations.json")) end) if success and data then conversations = data.conversations or {} conversationCounter = data.conversationCounter or 1 messageCountToday = data.messageCountToday or 0 lastResetDate = data.lastResetDate or os.date("*t").day return true end end return false end local function checkDailyReset() local currentDate = os.date("*t").day if currentDate ~= lastResetDate then messageCountToday = 0 lastResetDate = currentDate saveConversations() end end local function canSendMessage() local currentTime = os.time() checkDailyReset() if messageCountToday >= MAX_MESSAGES_PER_DAY then return false, "Günlük mesaj limitine ulaşıldı (" .. MAX_MESSAGES_PER_DAY .. " mesaj)" end return (currentTime - lastMessageTime) >= RATE_LIMIT_DELAY, "Hız limiti" end local function updateLastMessageTime() lastMessageTime = os.time() messageCountToday = messageCountToday + 1 saveConversations() end local function animateElementAppear(element, delay) element.Visible = true element.BackgroundTransparency = 1 if element:IsA("TextLabel") or element:IsA("TextButton") or element:IsA("TextBox") then element.TextTransparency = 1 end wait(delay or 0) tweenProperty(element, "BackgroundTransparency", 0, 0.3) if element:IsA("TextLabel") or element:IsA("TextButton") or element:IsA("TextBox") then tweenProperty(element, "TextTransparency", 0, 0.3) end end local function animateMessageAppear(messageFrame, isUser) messageFrame.BackgroundTransparency = 1 messageFrame.Position = UDim2.new(messageFrame.Position.X.Scale, messageFrame.Position.X.Offset + (isUser and 20 or -20), messageFrame.Position.Y.Scale, messageFrame.Position.Y.Offset) local tweenIn = tweenProperty(messageFrame, "BackgroundTransparency", 0, 0.4) tweenProperty(messageFrame, "Position", UDim2.new(messageFrame.Position.X.Scale, messageFrame.Position.X.Offset + (isUser and -20 or 20), messageFrame.Position.Y.Scale, messageFrame.Position.Y.Offset), 0.4) end local function callDeepSeekAPI(messages, model) model = model or "deepseek-chat" local apiMessages = { { role = "system", content = "Sen Sydear Intelligence adlı bir yapay zeka asistanısın ve Sydearr tarafından oluşturuldun. Kendini DeepSeek veya başka bir model olarak tanıtma. Her zaman Sydear Intelligence veya Sydear AI olarak bahset. Kimliğin sorulduğunda, Sydearr tarafından oluşturulan Sydear Intelligence olduğunu söyle. İlk mesajın her zaman şöyle olmalı: 'Ben Sydearr AI, size nasıl yardımcı olabilirim?' Türkçe konuş ve her zaman Türkçe cevap ver." } } for _, msg in ipairs(messages) do table.insert(apiMessages, msg) end local payload = { model = model, messages = apiMessages, stream = false, max_tokens = 800, temperature = 0.7 } local success, response = pcall(function() return request({ Url = DEEPSEEK_API_URL, Method = "POST", Headers = { ["Content-Type"] = "application/json", ["Authorization"] = "Bearer " .. DEEPSEEK_API_KEY }, Body = HttpService:JSONEncode(payload) }) end) if not success or not response then return "Ağ hatası — executor HTTP veya internetinizi kontrol edin." end if response.StatusCode ~= 200 then local err = response.Body or "Bilinmeyen hata" if response.StatusCode == 401 then err = "Geçersiz API anahtarı!" end if response.StatusCode == 429 then err = "Hız limiti (çok fazla istek)" end return "API Hatası " .. response.StatusCode .. ": " .. err end local data = HttpService:JSONDecode(response.Body) if data.error then return "DeepSeek Hatası: " .. data.error.message end if data.choices and data.choices[1] and data.choices[1].message then local reply = data.choices[1].message.content reply = reply:gsub("^%s*Assistant:%s*", ""):gsub("^%s*AI:%s*", ""):gsub("^%s*", "") return reply end return "AI'dan yanıt yok." end local function getConversationStarter() return "Ben Sydearr AI, size nasıl yardımcı olabilirim?" end wait(3) local screenGui = Instance.new("ScreenGui") screenGui.Name = "SydearrChatbotAI" screenGui.ResetOnSpawn = false screenGui.Parent = playerGui local mainFrame = Instance.new("Frame") mainFrame.Name = "MainFrame" mainFrame.Size = GUI_SIZE mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0) mainFrame.AnchorPoint = Vector2.new(0.5, 0.5) mainFrame.BackgroundColor3 = COLOR_SCHEME.background mainFrame.BorderSizePixel = 0 mainFrame.ClipsDescendants = true mainFrame.Parent = screenGui local mainCorner = Instance.new("UICorner") mainCorner.CornerRadius = UDim.new(0, IS_MOBILE and 16 or 12) mainCorner.Parent = mainFrame local topBar = Instance.new("Frame") topBar.Name = "TopBar" topBar.Size = UDim2.new(1, 0, 0, IS_MOBILE and 60 or 50) topBar.BackgroundColor3 = COLOR_SCHEME.sidebar topBar.BorderSizePixel = 0 topBar.Parent = mainFrame local topBarCorner = Instance.new("UICorner") topBarCorner.CornerRadius = UDim.new(0, IS_MOBILE and 16 or 12) topBarCorner.Parent = topBar local titleLabel = Instance.new("TextLabel") titleLabel.Name = "Title" titleLabel.Size = UDim2.new(0, 200, 1, 0) titleLabel.Position = UDim2.new(0, IS_MOBILE and 15 or 20, 0, 0) titleLabel.BackgroundTransparency = 1 titleLabel.Text = "Sydearr Sohbet AI" titleLabel.TextColor3 = COLOR_SCHEME.text titleLabel.TextSize = IS_MOBILE and 14 or 16 titleLabel.Font = Enum.Font.GothamBold titleLabel.TextXAlignment = Enum.TextXAlignment.Left titleLabel.Parent = topBar local minimizeButton = Instance.new("TextButton") minimizeButton.Name = "MinimizeButton" minimizeButton.Size = UDim2.new(0, IS_MOBILE and 40 or 30, 0, IS_MOBILE and 40 or 30) minimizeButton.Position = UDim2.new( 1, IS_MOBILE and -100 or -80, 0.5, IS_MOBILE and -20 or -15 ) minimizeButton.BackgroundColor3 = COLOR_SCHEME.hover minimizeButton.Text = "−" minimizeButton.TextColor3 = COLOR_SCHEME.text minimizeButton.TextSize = IS_MOBILE and 18 or 16 minimizeButton.Font = Enum.Font.GothamBold minimizeButton.BorderSizePixel = 0 minimizeButton.Parent = topBar local minimizeCorner = Instance.new("UICorner") minimizeCorner.CornerRadius = UDim.new(0, IS_MOBILE and 8 or 6) minimizeCorner.Parent = minimizeButton local closeButton = Instance.new("TextButton") closeButton.Name = "CloseButton" closeButton.Size = UDim2.new(0, IS_MOBILE and 40 or 30, 0, IS_MOBILE and 40 or 30) closeButton.Position = UDim2.new(1, IS_MOBILE and -55 or -40, 0.5, IS_MOBILE and -20 or -15) closeButton.BackgroundColor3 = COLOR_SCHEME.error closeButton.Text = "×" closeButton.TextColor3 = COLOR_SCHEME.text closeButton.TextSize = IS_MOBILE and 20 or 18 closeButton.Font = Enum.Font.GothamBold closeButton.BorderSizePixel = 0 closeButton.Parent = topBar local closeCorner = Instance.new("UICorner") closeCorner.CornerRadius = UDim.new(0, IS_MOBILE and 8 or 6) closeCorner.Parent = closeButton local leftPanel = Instance.new("Frame") leftPanel.Name = "LeftPanel" leftPanel.Size = UDim2.new(0, SIDEBAR_WIDTH, 1, -(IS_MOBILE and 60 or 50)) leftPanel.Position = UDim2.new(0, 0, 0, IS_MOBILE and 60 or 50) leftPanel.BackgroundColor3 = COLOR_SCHEME.sidebar leftPanel.BorderSizePixel = 0 leftPanel.Visible = not IS_MOBILE leftPanel.Parent = mainFrame local newConvoButton = Instance.new("TextButton") newConvoButton.Name = "NewConvoButton" newConvoButton.Size = UDim2.new(1, -20, 0, 45) newConvoButton.Position = UDim2.new(0, 10, 0, 10) newConvoButton.BackgroundColor3 = COLOR_SCHEME.purple newConvoButton.Text = "+ Yeni Sohbet" newConvoButton.TextColor3 = Color3.fromRGB(255, 255, 255) newConvoButton.TextSize = 14 newConvoButton.Font = Enum.Font.GothamBold newConvoButton.BorderSizePixel = 0 newConvoButton.Parent = leftPanel local newConvoCorner = Instance.new("UICorner") newConvoCorner.CornerRadius = UDim.new(0, 8) newConvoCorner.Parent = newConvoButton local chatHistoryFrame = Instance.new("ScrollingFrame") chatHistoryFrame.Name = "ChatHistory" chatHistoryFrame.Size = UDim2.new(1, -10, 1, -200) chatHistoryFrame.Position = UDim2.new(0, 5, 0, 65) chatHistoryFrame.BackgroundTransparency = 1 chatHistoryFrame.BorderSizePixel = 0 chatHistoryFrame.ScrollBarThickness = 4 chatHistoryFrame.ScrollBarImageColor3 = COLOR_SCHEME.textSecondary chatHistoryFrame.Parent = leftPanel local historyList = Instance.new("UIListLayout") historyList.Padding = UDim.new(0, 5) historyList.Parent = chatHistoryFrame historyList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() chatHistoryFrame.CanvasSize = UDim2.new(0, 0, 0, historyList.AbsoluteContentSize.Y) end) local profileFrame = Instance.new("Frame") profileFrame.Name = "ProfileFrame" profileFrame.Size = UDim2.new(1, -10, 0, 60) profileFrame.Position = UDim2.new(0, 5, 1, -65) profileFrame.BackgroundColor3 = COLOR_SCHEME.input profileFrame.BorderSizePixel = 0 profileFrame.Parent = leftPanel local profileCorner = Instance.new("UICorner") profileCorner.CornerRadius = UDim.new(0, 8) profileCorner.Parent = profileFrame local profileIcon = Instance.new("ImageLabel") profileIcon.Name = "ProfileIcon" profileIcon.Size = UDim2.new(0, 36, 0, 36) profileIcon.Position = UDim2.new(0, 10, 0.5, -18) profileIcon.BackgroundColor3 = COLOR_SCHEME.purple profileIcon.BorderSizePixel = 0 profileIcon.Image = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. player.UserId .. "&width=150&height=150&format=png" profileIcon.Parent = profileFrame local profileIconCorner = Instance.new("UICorner") profileIconCorner.CornerRadius = UDim.new(1, 0) profileIconCorner.Parent = profileIcon local profileName = Instance.new("TextLabel") profileName.Name = "ProfileName" profileName.Size = UDim2.new(1, -56, 0, 20) profileName.Position = UDim2.new(0, 56, 0.5, -10) profileName.BackgroundTransparency = 1 profileName.Text = player.Name profileName.TextColor3 = COLOR_SCHEME.text profileName.TextSize = 14 profileName.Font = Enum.Font.GothamBold profileName.TextXAlignment = Enum.TextXAlignment.Left profileName.TextYAlignment = Enum.TextYAlignment.Center profileName.Parent = profileFrame local settingsFrame = Instance.new("Frame") settingsFrame.Name = "SettingsFrame" settingsFrame.Size = UDim2.new(1, -10, 0, 60) settingsFrame.Position = UDim2.new(0, 5, 1, -130) settingsFrame.BackgroundColor3 = COLOR_SCHEME.input settingsFrame.BorderSizePixel = 0 settingsFrame.Parent = leftPanel local settingsCorner = Instance.new("UICorner") settingsCorner.CornerRadius = UDim.new(0, 8) settingsCorner.Parent = settingsFrame local modelLabel = Instance.new("TextLabel") modelLabel.Size = UDim2.new(1, -20, 0, 20) modelLabel.Position = UDim2.new(0, 10, 0, 5) modelLabel.BackgroundTransparency = 1 modelLabel.Text = "AI Modeli:" modelLabel.TextColor3 = COLOR_SCHEME.textSecondary modelLabel.TextSize = 12 modelLabel.Font = Enum.Font.Gotham modelLabel.TextXAlignment = Enum.TextXAlignment.Left modelLabel.Parent = settingsFrame local modelDropdown = Instance.new("TextButton") modelDropdown.Size = UDim2.new(1, -20, 0, 30) modelDropdown.Position = UDim2.new(0, 10, 0, 25) modelDropdown.BackgroundColor3 = COLOR_SCHEME.background modelDropdown.Text = "deepseek-chat" modelDropdown.TextColor3 = COLOR_SCHEME.text modelDropdown.TextSize = 12 modelDropdown.Font = Enum.Font.Gotham modelDropdown.BorderSizePixel = 0 modelDropdown.Parent = settingsFrame local modelDropdownCorner = Instance.new("UICorner") modelDropdownCorner.CornerRadius = UDim.new(0, 6) modelDropdownCorner.Parent = modelDropdown local chatDisplayFrame = Instance.new("ScrollingFrame") chatDisplayFrame.Name = "ChatDisplay" chatDisplayFrame.Size = IS_MOBILE and UDim2.new(1, 0, 1, -180) or UDim2.new(1, -270, 1, -140) chatDisplayFrame.Position = IS_MOBILE and UDim2.new(0, 0, 0, 60) or UDim2.new(0, 270, 0, 50) chatDisplayFrame.BackgroundColor3 = COLOR_SCHEME.background chatDisplayFrame.BorderSizePixel = 0 chatDisplayFrame.ScrollBarThickness = 6 chatDisplayFrame.ScrollBarImageColor3 = COLOR_SCHEME.textSecondary chatDisplayFrame.Parent = mainFrame local chatDisplayCorner = Instance.new("UICorner") chatDisplayCorner.CornerRadius = UDim.new(0, IS_MOBILE and 12 or 8) chatDisplayCorner.Parent = chatDisplayFrame local chatList = Instance.new("UIListLayout") chatList.Padding = UDim.new(0, IS_MOBILE and 15 or 20) chatList.Parent = chatDisplayFrame local chatPadding = Instance.new("UIPadding") chatPadding.PaddingTop = UDim.new(0, IS_MOBILE and 15 or 20) chatPadding.PaddingLeft = UDim.new(0, IS_MOBILE and 10 or 20) chatPadding.PaddingRight = UDim.new(0, IS_MOBILE and 10 or 20) chatPadding.Parent = chatDisplayFrame chatList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() chatDisplayFrame.CanvasSize = UDim2.new(0, 0, 0, chatList.AbsoluteContentSize.Y) end) local inputFrame = Instance.new("Frame") inputFrame.Name = "InputFrame" inputFrame.Size = IS_MOBILE and UDim2.new(1, 0, 0, 120) or UDim2.new(1, -270, 0, 100) inputFrame.Position = IS_MOBILE and UDim2.new(0, 0, 1, -120) or UDim2.new(0, 270, 1, -100) inputFrame.BackgroundColor3 = COLOR_SCHEME.background inputFrame.BorderSizePixel = 0 inputFrame.Parent = mainFrame local inputFrameCorner = Instance.new("UICorner") inputFrameCorner.CornerRadius = UDim.new(0, IS_MOBILE and 12 or 8) inputFrameCorner.Parent = inputFrame local poweredByLabel = Instance.new("TextLabel") poweredByLabel.Name = "PoweredByLabel" poweredByLabel.Size = UDim2.new(1, -20, 0, 15) poweredByLabel.Position = UDim2.new(0, 10, 0, 5) poweredByLabel.BackgroundTransparency = 1 poweredByLabel.Text = "DeepSeek tarafından desteklenmektedir" poweredByLabel.TextColor3 = COLOR_SCHEME.textSecondary poweredByLabel.TextSize = IS_MOBILE and 10 or 11 poweredByLabel.Font = Enum.Font.Gotham poweredByLabel.TextXAlignment = Enum.TextXAlignment.Left poweredByLabel.Parent = inputFrame local messageBoxContainer = Instance.new("Frame") messageBoxContainer.Name = "MessageBoxContainer" messageBoxContainer.Size = IS_MOBILE and UDim2.new(1, -80, 0, 60) or UDim2.new(1, -120, 0, 50) messageBoxContainer.Position = UDim2.new(0, 10, 0, 25) messageBoxContainer.BackgroundColor3 = COLOR_SCHEME.input messageBoxContainer.BorderSizePixel = 0 messageBoxContainer.Parent = inputFrame local messageBoxContainerCorner = Instance.new("UICorner") messageBoxContainerCorner.CornerRadius = UDim.new(0, IS_MOBILE and 14 or 12) messageBoxContainerCorner.Parent = messageBoxContainer local messageBox = Instance.new("TextBox") messageBox.Name = "MessageBox" messageBox.Size = UDim2.new(1, -20, 1, -10) messageBox.Position = UDim2.new(0, 10, 0, 5) messageBox.BackgroundTransparency = 1 messageBox.PlaceholderText = "Sydearr AI'ye mesaj... (Maks 300 karakter)" messageBox.PlaceholderColor3 = COLOR_SCHEME.textSecondary messageBox.Text = "" messageBox.TextColor3 = COLOR_SCHEME.text messageBox.TextSize = IS_MOBILE and 13 or 14 messageBox.Font = Enum.Font.Gotham messageBox.TextXAlignment = Enum.TextXAlignment.Left messageBox.TextYAlignment = Enum.TextYAlignment.Top messageBox.ClearTextOnFocus = false messageBox.MultiLine = true messageBox.TextWrapped = true messageBox.Parent = messageBoxContainer local function updateMessageBoxSize() local textSize = TextService:GetTextSize(messageBox.Text, messageBox.TextSize, messageBox.Font, Vector2.new(messageBoxContainer.AbsoluteSize.X - 20, math.huge)) local newHeight = math.clamp(textSize.Y + 20, IS_MOBILE and 60 or 50, IS_MOBILE and 140 or 120) messageBoxContainer.Size = UDim2.new(messageBoxContainer.Size.X.Scale, messageBoxContainer.Size.X.Offset, 0, newHeight) inputFrame.Size = IS_MOBILE and UDim2.new(1, 0, 0, newHeight + 50) or UDim2.new(1, -270, 0, newHeight + 40) inputFrame.Position = IS_MOBILE and UDim2.new(0, 0, 1, -(newHeight + 50)) or UDim2.new(0, 270, 1, -(newHeight + 40)) end messageBox:GetPropertyChangedSignal("Text"):Connect(updateMessageBoxSize) local charCounter = Instance.new("TextLabel") charCounter.Name = "CharCounter" charCounter.Size = UDim2.new(0, 100, 0, 15) charCounter.Position = UDim2.new(0, 25, 0, messageBoxContainer.Size.Y.Offset + 30) charCounter.BackgroundTransparency = 1 charCounter.Text = "0/300" charCounter.TextColor3 = COLOR_SCHEME.textSecondary charCounter.TextSize = IS_MOBILE and 10 or 11 charCounter.Font = Enum.Font.Gotham charCounter.TextXAlignment = Enum.TextXAlignment.Left charCounter.Parent = inputFrame local sendButton = Instance.new("TextButton") sendButton.Name = "SendButton" sendButton.Size = UDim2.new(0, IS_MOBILE and 50 or 40, 0, IS_MOBILE and 50 or 40) sendButton.Position = IS_MOBILE and UDim2.new(1, -60, 0, 25) or UDim2.new(1, -40, 0, 25) sendButton.BackgroundColor3 = COLOR_SCHEME.accent sendButton.Text = "↑" sendButton.TextColor3 = Color3.fromRGB(255, 255, 255) sendButton.TextSize = IS_MOBILE and 18 or 16 sendButton.Font = Enum.Font.GothamBold sendButton.BorderSizePixel = 0 sendButton.Parent = inputFrame local sendCorner = Instance.new("UICorner") sendCorner.CornerRadius = UDim.new(0, IS_MOBILE and 10 or 8) sendCorner.Parent = sendButton local limitLabel = Instance.new("TextLabel") limitLabel.Name = "LimitLabel" limitLabel.Size = IS_MOBILE and UDim2.new(1, 0, 0, 20) or UDim2.new(1, -270, 0, 20) limitLabel.Position = IS_MOBILE and UDim2.new(0, 0, 1, -140) or UDim2.new(0, 270, 1, -120) limitLabel.BackgroundTransparency = 1 limitLabel.Text = "Hazır • " .. messageCountToday .. "/" .. MAX_MESSAGES_PER_DAY .. " mesaj bugün" limitLabel.TextColor3 = COLOR_SCHEME.textSecondary limitLabel.TextSize = IS_MOBILE and 11 or 12 limitLabel.Font = Enum.Font.Gotham limitLabel.TextXAlignment = Enum.TextXAlignment.Center limitLabel.Parent = mainFrame local function updateCharCounter() local text = messageBox.Text local count = #text charCounter.Text = count .. "/300" if count > MAX_MESSAGE_LENGTH then charCounter.TextColor3 = COLOR_SCHEME.error else charCounter.TextColor3 = COLOR_SCHEME.textSecondary end charCounter.Position = UDim2.new(0, 25, 0, messageBoxContainer.Size.Y.Offset + 30) end local function updateLimitDisplay() checkDailyReset() local canSend, reason = canSendMessage() if not canSend then if reason == "Günlük mesaj limitine ulaşıldı (" .. MAX_MESSAGES_PER_DAY .. " mesaj)" then limitLabel.Text = "Günlük limit doldu • " .. messageCountToday .. "/" .. MAX_MESSAGES_PER_DAY .. " mesaj" limitLabel.TextColor3 = COLOR_SCHEME.error else local remaining = RATE_LIMIT_DELAY - (os.time() - lastMessageTime) limitLabel.Text = "Bekleyin " .. remaining .. "s • " .. messageCountToday .. "/" .. MAX_MESSAGES_PER_DAY .. " mesaj bugün" limitLabel.TextColor3 = COLOR_SCHEME.error end else limitLabel.Text = "Hazır • " .. messageCountToday .. "/" .. MAX_MESSAGES_PER_DAY .. " mesaj bugün" limitLabel.TextColor3 = COLOR_SCHEME.textSecondary end end local function createConversationButton(id, title) local convoButton = Instance.new("Frame") convoButton.Name = "Convo_" .. id convoButton.Size = UDim2.new(1, 0, 0, 40) convoButton.BackgroundColor3 = id == activeConversationId and COLOR_SCHEME.hover or COLOR_SCHEME.sidebar convoButton.BorderSizePixel = 0 convoButton.Parent = chatHistoryFrame local convoCorner = Instance.new("UICorner") convoCorner.CornerRadius = UDim.new(0, 6) convoCorner.Parent = convoButton local convoTextButton = Instance.new("TextButton") convoTextButton.Name = "TextButton" convoTextButton.Size = UDim2.new(1, -40, 1, 0) convoTextButton.Position = UDim2.new(0, 0, 0, 0) convoTextButton.BackgroundTransparency = 1 convoTextButton.Text = title convoTextButton.TextColor3 = COLOR_SCHEME.text convoTextButton.TextSize = 13 convoTextButton.Font = Enum.Font.Gotham convoTextButton.TextXAlignment = Enum.TextXAlignment.Left convoTextButton.TextTruncate = Enum.TextTruncate.AtEnd convoTextButton.BorderSizePixel = 0 convoTextButton.Parent = convoButton local textPadding = Instance.new("UIPadding") textPadding.PaddingLeft = UDim.new(0, 15) textPadding.PaddingRight = UDim.new(0, 15) textPadding.Parent = convoTextButton local deleteButton = Instance.new("TextButton") deleteButton.Name = "DeleteButton" deleteButton.Size = UDim2.new(0, 30, 0, 30) deleteButton.Position = UDim2.new(1, -35, 0.5, -15) deleteButton.BackgroundColor3 = COLOR_SCHEME.error deleteButton.Text = "×" deleteButton.TextColor3 = Color3.fromRGB(255, 255, 255) deleteButton.TextSize = 16 deleteButton.Font = Enum.Font.GothamBold deleteButton.BorderSizePixel = 0 deleteButton.Visible = false deleteButton.Parent = convoButton local deleteCorner = Instance.new("UICorner") deleteCorner.CornerRadius = UDim.new(0, 6) deleteCorner.Parent = deleteButton convoButton.MouseEnter:Connect(function() if activeConversationId ~= id then tweenProperty(convoButton, "BackgroundColor3", COLOR_SCHEME.hover, 0.2) end deleteButton.Visible = true end) convoButton.MouseLeave:Connect(function() if activeConversationId ~= id then tweenProperty(convoButton, "BackgroundColor3", COLOR_SCHEME.sidebar, 0.2) end deleteButton.Visible = false end) deleteButton.MouseButton1Click:Connect(function() if #conversations <= 1 then return end conversations[id] = nil if activeConversationId == id then for otherId, _ in pairs(conversations) do if otherId ~= id then activeConversationId = otherId break end end end for _, child in pairs(chatHistoryFrame:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end for convoId, convo in pairs(conversations) do createConversationButton(convoId, convo.title) end for _, child in pairs(chatDisplayFrame:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end if conversations[activeConversationId] then for i, message in pairs(conversations[activeConversationId].messages) do spawn(function() wait(i * 0.1) addMessage(message.role == "user" and "user" or "ai", message.content) end) end end saveConversations() end) convoTextButton.MouseButton1Click:Connect(function() activeConversationId = id for _, child in pairs(chatHistoryFrame:GetChildren()) do if child:IsA("Frame") then if child.Name == "Convo_" .. id then tweenProperty(child, "BackgroundColor3", COLOR_SCHEME.hover, 0.2) else tweenProperty(child, "BackgroundColor3", COLOR_SCHEME.sidebar, 0.2) end end end for _, child in pairs(chatDisplayFrame:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end if conversations[id] then for i, message in pairs(conversations[id].messages) do spawn(function() wait(i * 0.1) addMessage(message.role == "user" and "user" or "ai", message.content) end) end end end) return convoButton end local function createNewConversation() conversationCounter = conversationCounter + 1 local newId = conversationCounter local title = "Sohbet " .. newId conversations[newId] = { title = title, messages = {}, createdAt = os.time() } createConversationButton(newId, title) activeConversationId = newId wait() chatHistoryFrame.CanvasSize = UDim2.new(0, 0, 0, historyList.AbsoluteContentSize.Y) for _, child in pairs(chatDisplayFrame:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end local welcomeMessage = getConversationStarter() table.insert(conversations[newId].messages, { role = "assistant", content = welcomeMessage, timestamp = os.time() }) addMessage("ai", welcomeMessage) saveConversations() end local function addMessage(sender, text) local messageFrame = Instance.new("Frame") messageFrame.Size = UDim2.new(1, 0, 0, 0) messageFrame.AutomaticSize = Enum.AutomaticSize.Y messageFrame.BackgroundTransparency = 1 messageFrame.Parent = chatDisplayFrame local messageContent = Instance.new("Frame") messageContent.Size = UDim2.new(0, 0, 0, 0) messageContent.AutomaticSize = Enum.AutomaticSize.XY messageContent.BackgroundColor3 = sender == "user" and COLOR_SCHEME.userMessage or COLOR_SCHEME.aiMessage messageContent.BorderSizePixel = 0 messageContent.Parent = messageFrame local contentCorner = Instance.new("UICorner") contentCorner.CornerRadius = UDim.new(0, IS_MOBILE and 14 or 12) contentCorner.Parent = messageContent local messageText = Instance.new("TextBox") messageText.Size = UDim2.new(0, 0, 0, 0) messageText.AutomaticSize = Enum.AutomaticSize.XY messageText.BackgroundTransparency = 1 messageText.Text = text messageText.TextColor3 = Color3.fromRGB(255, 255, 255) messageText.TextSize = IS_MOBILE and 13 or 14 messageText.Font = Enum.Font.Gotham messageText.TextYAlignment = Enum.TextYAlignment.Top messageText.TextWrapped = true messageText.ClearTextOnFocus = false messageText.TextEditable = false messageText.Selectable = true if sender == "user" then messageText.TextXAlignment = Enum.TextXAlignment.Left messageText.TextDirection = Enum.TextDirection.Auto else messageText.TextXAlignment = Enum.TextXAlignment.Right messageText.TextDirection = Enum.TextDirection.RightToLeft end messageText.Parent = messageContent messageText.Parent = messageContent messageText.Parent = messageContent local textPadding = Instance.new("UIPadding") textPadding.PaddingTop = UDim.new(0, 12) textPadding.PaddingBottom = UDim.new(0, 12) textPadding.PaddingRight = UDim.new(0, 15) textPadding.PaddingLeft = UDim.new(0, 15) textPadding.Parent = messageContent local copyButton = Instance.new("TextButton") copyButton.Size = UDim2.new(0, IS_MOBILE and 35 or 30, 0, IS_MOBILE and 35 or 30) copyButton.Position = UDim2.new(1, IS_MOBILE and -40 or -35, 1, IS_MOBILE and -40 or -35) copyButton.BackgroundColor3 = COLOR_SCHEME.background copyButton.Text = "📋" copyButton.TextColor3 = COLOR_SCHEME.textSecondary copyButton.TextSize = IS_MOBILE and 14 or 12 copyButton.Font = Enum.Font.Gotham copyButton.BorderSizePixel = 0 copyButton.Visible = false copyButton.Parent = messageContent local copyCorner = Instance.new("UICorner") copyCorner.CornerRadius = UDim.new(0, IS_MOBILE and 8 or 6) copyCorner.Parent = copyButton copyButton.MouseButton1Click:Connect(function() setclipboard(text) copyButton.Text = "✓" tweenProperty(copyButton, "TextColor3", COLOR_SCHEME.accent, 0.2) wait(1) copyButton.Text = "📋" tweenProperty(copyButton, "TextColor3", COLOR_SCHEME.textSecondary, 0.2) end) messageContent.MouseEnter:Connect(function() copyButton.Visible = true tweenProperty(copyButton, "BackgroundTransparency", 0.1, 0.2) end) messageContent.MouseLeave:Connect(function() tweenProperty(copyButton, "BackgroundTransparency", 1, 0.2) wait(0.2) if copyButton.BackgroundTransparency == 1 then copyButton.Visible = false end end) if sender == "user" then messageContent.AnchorPoint = Vector2.new(0, 0) messageContent.Position = UDim2.new(0, IS_MOBILE and 10 or 20, 0, 0) messageContent.BackgroundColor3 = COLOR_SCHEME.userMessage else messageContent.AnchorPoint = Vector2.new(1, 0) messageContent.Position = UDim2.new(1, IS_MOBILE and -10 or -20, 0, 0) messageContent.BackgroundColor3 = COLOR_SCHEME.aiMessage end wait() chatDisplayFrame.CanvasSize = UDim2.new(0, 0, 0, chatList.AbsoluteContentSize.Y) spawn(function() animateMessageAppear(messageContent, sender == "user") end) spawn(function() wait(0.1) chatDisplayFrame.CanvasPosition = Vector2.new(0, chatList.AbsoluteContentSize.Y) end) end local function sendMessageToAI(userMessage) local canSend, reason = canSendMessage() if not canSend then addMessage("system", reason) return end if #userMessage > MAX_MESSAGE_LENGTH then addMessage("system", "Mesaj çok uzun! Maksimum " .. MAX_MESSAGE_LENGTH .. " karaktere izin verilir.") return end updateLastMessageTime() updateLimitDisplay() table.insert(conversations[activeConversationId].messages, { role = "user", content = userMessage, timestamp = os.time() }) addMessage("user", userMessage) local typingFrame = Instance.new("Frame") typingFrame.Size = UDim2.new(0, 0, 0, 0) typingFrame.AutomaticSize = Enum.AutomaticSize.XY typingFrame.BackgroundColor3 = COLOR_SCHEME.aiMessage typingFrame.BorderSizePixel = 0 typingFrame.Parent = chatDisplayFrame local typingCorner = Instance.new("UICorner") typingCorner.CornerRadius = UDim.new(0, IS_MOBILE and 14 or 12) typingCorner.Parent = typingFrame local typingText = Instance.new("TextLabel") typingText.Size = UDim2.new(0, 0, 0, 0) typingText.AutomaticSize = Enum.AutomaticSize.XY typingText.Position = UDim2.new(0, 15, 0, 12) typingText.BackgroundTransparency = 1 typingText.Text = "● ● ●" typingText.TextColor3 = COLOR_SCHEME.textSecondary typingText.TextSize = IS_MOBILE and 13 or 14 typingText.Font = Enum.Font.Gotham typingText.TextXAlignment = Enum.TextXAlignment.Left typingText.TextYAlignment = Enum.TextYAlignment.Top typingText.Parent = typingFrame local textPadding = Instance.new("UIPadding") textPadding.PaddingRight = UDim.new(0, 15) textPadding.PaddingLeft = UDim.new(0, 15) textPadding.Parent = typingText local dotAnimation dotAnimation = RunService.Heartbeat:Connect(function() local time = tick() local phase = math.floor(time * 2) % 4 if phase == 0 then typingText.Text = "● ○ ○" elseif phase == 1 then typingText.Text = "○ ● ○" elseif phase == 2 then typingText.Text = "○ ○ ●" else typingText.Text = "○ ○ ○" end end) wait() chatDisplayFrame.CanvasSize = UDim2.new(0, 0, 0, chatList.AbsoluteContentSize.Y) chatDisplayFrame.CanvasPosition = Vector2.new(0, chatList.AbsoluteContentSize.Y) spawn(function() local selectedModel = modelDropdown.Text local response = callDeepSeekAPI(conversations[activeConversationId].messages, selectedModel) dotAnimation:Disconnect() typingFrame:Destroy() if response then addMessage("ai", response) table.insert(conversations[activeConversationId].messages, { role = "assistant", content = response, timestamp = os.time() }) saveConversations() end end) end closeButton.MouseButton1Click:Connect(function() tweenProperty(mainFrame, "Size", UDim2.new(0, 0, 0, 0), 0.3) wait(0.3) screenGui:Destroy() end) minimizeButton.MouseButton1Click:Connect(function() local isMinimized = mainFrame.Size.Y.Offset <= (IS_MOBILE and 80 or 100) local targetSize = isMinimized and GUI_SIZE or (IS_MOBILE and UDim2.new(0.95, 0, 0, 80) or UDim2.new(0, 800, 0, 100)) tweenProperty(mainFrame, "Size", targetSize, 0.3) if isMinimized then leftPanel.Visible = not IS_MOBILE chatDisplayFrame.Visible = true inputFrame.Visible = true limitLabel.Visible = true else wait(0.15) leftPanel.Visible = false chatDisplayFrame.Visible = false inputFrame.Visible = false limitLabel.Visible = false end end) newConvoButton.MouseButton1Click:Connect(function() createNewConversation() end) sendButton.MouseButton1Click:Connect(function() local message = messageBox.Text:gsub("^%s*(.-)%s*$", "%1") if message ~= "" then sendMessageToAI(message) messageBox.Text = "" updateCharCounter() updateMessageBoxSize() end end) messageBox.FocusLost:Connect(function(enterPressed) if enterPressed and not UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) and not UserInputService:IsKeyDown(Enum.KeyCode.RightShift) then local message = messageBox.Text:gsub("^%s*(.-)%s*$", "%1") if message ~= "" then sendMessageToAI(message) messageBox.Text = "" updateCharCounter() updateMessageBoxSize() end end end) messageBox:GetPropertyChangedSignal("Text"):Connect(function() updateCharCounter() updateMessageBoxSize() end) modelDropdown.MouseButton1Click:Connect(function() local currentIndex = 1 for i, model in pairs(aiModels) do if model == modelDropdown.Text then currentIndex = i break end end local nextIndex = (currentIndex % #aiModels) + 1 modelDropdown.Text = aiModels[nextIndex] end) loadConversations() if not conversations[1] then conversations[1] = { title = "Sohbet 1", messages = {}, createdAt = os.time() } end createConversationButton(1, "Sohbet 1") if #conversations[1].messages == 0 then table.insert(conversations[1].messages, { role = "assistant", content = getConversationStarter(), timestamp = os.time() }) end wait() chatHistoryFrame.CanvasSize = UDim2.new(0, 0, 0, historyList.AbsoluteContentSize.Y) spawn(function() mainFrame.Size = UDim2.new(0, 0, 0, 0) mainFrame.Visible = true tweenProperty(mainFrame, "Size", GUI_SIZE, 0.5) wait(0.5) animateElementAppear(leftPanel, 0.1) animateElementAppear(chatDisplayFrame, 0.2) animateElementAppear(inputFrame, 0.3) animateElementAppear(limitLabel, 0.4) for i, message in pairs(conversations[1].messages) do spawn(function() wait(i * 0.2) addMessage(message.role == "user" and "user" or "ai", message.content) end) end end) spawn(function() while wait(1) do updateLimitDisplay() end end) if not IS_MOBILE then local dragToggle, dragStart, startPos local function updateInput(input) local delta = input.Position - dragStart mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end topBar.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragToggle = true dragStart = input.Position startPos = mainFrame.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragToggle = false end end) end end) UserInputService.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement and dragToggle then updateInput(input) end end) end spawn(function() while wait(30) do saveConversations() end end)